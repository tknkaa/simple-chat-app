{
  "name": "simple-chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "trap 'kill 0' EXIT; (cd frontend && npm run start) & (cd backend && npx tsc && node dist/server.js) & wait"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "prettier": "^3.4.1"
  }
}
